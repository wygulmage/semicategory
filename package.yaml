name:                semicategory
version:             0.1.0.0
github:              "wygulmage/semicategory"
license:             BSD3
author:              "Keith Wygulmage"
maintainer:          "wygulmage@gmail.com"
copyright:           "2018 Keith Wygulmage"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/wygulmage/semicategory#readme>

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src

executables:
  # semicategory-exe:
  #   main:                Main.hs
  #   source-dirs:         app
  #   ghc-options:
  #   - -threaded
  #   - -rtsopts
  #   - -with-rtsopts=-N
  #   dependencies:
  #   - semicategory

tests:
  semicategory-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - semicategory
