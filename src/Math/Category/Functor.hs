{-# LANGUAGE NoImplicitPrelude #-}

module Math.Category.Functor
  (Functor(..), Bifunctor, Profunctor, bimap, dimap)
  where

import Math.Category (Functor(..), Bifunctor, Profunctor, bimap, dimap)

-- dummy to avoid circular module dependencies
